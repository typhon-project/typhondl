database VehicleDataDB : mongosharded {
	helm {
		repoName = bitnami ;
		repoAddress = https://charts.bitnami.com/bitnami ;
		chartName = mongodb-sharded ;
	}
	credentials {
		username = root ;
		password = km2ZKCTDnFkegtkf ;
	}
}
