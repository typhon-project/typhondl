platformtype default //TODO (e.g. AWS)
containertype Docker
dbtype relationaldb
dbtype documentdb

databases {
	mariadb productdb {
		image = "mariadb:latest";
		MYSQL_USER = me;
		MYSQL_PASSWORD = safe;
		MYSQL_ROOT_PASSWORD = evenSafer;
		MYSQL_DATABASE = wow;
		// TODO: evironment etc.
	}
	mongo orderdb {
		image = "mongo:latest";
		MONGO_INITDB_ROOT_USERNAME = me;
		MONGO_INITDB_ROOT_PASSWORD = safe;
		// TODO: evironment etc.
	}
	mariadb userdb {
		image = "mariadb:latest";
		MYSQL_USER = me;
		MYSQL_PASSWORD = safe;
		MYSQL_ROOT_PASSWORD = evenSafer;
		MYSQL_DATABASE = wow;
		// TODO: evironment etc.
	}
}

platform platformname : default { //TODO
	cluster clustername { //TODO
		application name {
			container productdb : Docker {
				database : productdb
				dbType : relationaldb
				// TODO: volumes, networks, ports etc.
			}
			container orderdb : Docker {
				database : orderdb
				dbType : documentdb
				// TODO: volumes, networks, ports etc.
			}
			container userdb : Docker {
				database : userdb
				dbType : relationaldb
				// TODO: volumes, networks, ports etc.
			}
		}
	}
}
