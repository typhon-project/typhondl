database  productdb : mariadb {
	image = "mariadb:latest";
	password = mypassword;
}
database orderdb : mongo {
	image = "mongo:latest";
	password = mypassword;
}
database  userdb : mariadb {
	image = "mariadb:1.6";
	password = mypassword;
}
